SEND_TELE = False # Set to true if sending Tele, if false it will just print to console
SEND_INTERVAL = '24h' # Format 's' for second, 'm' for minute, 'h' for hour, 'd' for day
VIEW_NUM = 25 # Max Symbols Viewed per comparison

# Used for telegram bot updates
token = ''  # Insert token obtained from @BotFather here
chat_id = 0 # Insert Chat ID

# Debug Params (Avoid Modifying if no error)
SEND_TELEGRAM_FAIL_INTERVAL = 2